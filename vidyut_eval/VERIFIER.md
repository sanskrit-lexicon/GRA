# H5147 eval — independent verification

_Created: 19-09-2026 · Last updated: 19-09-2026_

Verifier lane (independent re-derivation, not trusting committed artifacts). Worktree: GRA-h5147-drain, branch `h5147-drain`, start state `git status --porcelain` clean.

## Verifier

### 1. Determinism of the fixed sample — PASS

Ran (system python3, no vidyut needed):

```
GRA_TXT=/Users/mac/Documents/GitHub/csl-orig/v02/gra/gra.txt python3 vidyut_eval/make_sample.py
```

Output: `headwords parsed: 11836 · verbs01 verb cases parsed: 902 · stride: 39 sample size: 300 · verbs01 verbs inside slice-A sample: 30`.
`git diff --stat` after the run: empty. Byte-identity additionally confirmed with `cmp` for `sample_headwords.tsv`, `verb_set.tsv`, `vidyut_sample.tsv`, `vidyut_verbs.tsv` against a pre-run snapshot: all IDENTICAL.

### 2. Numbers re-run — PASS

Ran:

```
/tmp/opencode/vidyut-env/bin/python vidyut_eval/run_vidyut.py
/tmp/opencode/vidyut-env/bin/python vidyut_eval/compare.py
```

`run_vidyut.py` printed `slice B: derive ok 0 / kosha-hit 0 / lemma==k1 0` (round-trip blocked by API 0.4.0, as REPORT.md states). After both runs `git diff --stat` and `git status --porcelain` empty — `eval_summary.md`, both discrepancy TSVs and both output TSVs regenerated byte-identical.

Independent recomputation from the TSVs (separate python script, not compare.py):

- Slice A (n=300): vidyut any-hit **253/300 (84.3%)** ✓; verbs01 verb **30/300** ✓; vidyut verb-hit **11/300** ✓; confusion (verbs01-verb ∩ vidyut-verb) = **0**, (verbs01-verb, vidyut-not-verb) = 30, (verbs01-not-verb, vidyut-verb) = 11, (not,not) = 259 → precision **0.0%**, recall **0/30 = 0.0%** ✓
- Slice B (n=902): kosha ANY **325 (36.0%)** ✓; Tinanta **4 (0.4%)** ✓; Tinanta lemma == mw root **3 (0.3% of all, 75.0% of verb-hits)** ✓
- Round-trip: rt derive_ok rows = **0**, rt lookup>0 rows = **0** — no round-trip numbers exist anywhere ✓

### 3. Hand spot-checks (live kosha probes, 5 total) — PASS

- `akz` → **0 entries** ✓ (claim holds)
- `zAs` → 15 entries, all `PyPadaEntry_Subanta`, all lemma `za` — **only Subanta lemma `za`** ✓
- `SrA` → 62 entries; exactly **one** Tinanta entry, lemma **`SrA`** ✓ (rest Subanta homonyms)
- `vidyut_sample.tsv` row L=40 (`akz`, committed n=0, query `akz`) → live probe n=0, query `akz`, MATCH ✓
- `vidyut_sample.tsv` row L=1 (`a`, committed n=6, query `a`) → live probe n=6, lemmas set identical, MATCH ✓

### 4. Report consistency — PASS

- REPORT.md tables match `eval_summary.md` line-for-line: 253/300 = 84.3%, 30/300 = 10.0%, 11/300 = 3.7%, 0 из 30, precision/recall 0.0%/0.0%, slice B 325/36.0%, 4/0.4%, 3 (0.3% / 75% verb-hits) ✓
- The 4 Tinanta hits in `vidyut_verbs.tsv` are exactly `Ar→f` (wrong), `jYA→jYA`, `pAtu→pA`, `SrA→SrA` — matches REPORT.md's example line ✓
- Verdict text («vidyut НЕ брать», quality incomparable) is supported by the numbers: recall 0/30 on slice A, 36.0% any-hit and 0.4% verb-hit on slice B ✓
- No round-trip numbers claimed anywhere; REPORT.md explicitly says the round-trip metric is absent (API 0.4.0) and rt columns are all 0 ✓
- `discrepancies_sliceA.tsv` header-only (0 both-verb rows — consistent with confusion cell 0); `discrepancies_sliceB.tsv` exactly one row (`Ar`, 18 entries / 4 tinanta / lemma `f`) — consistent with 4 tinanta hits of which 3 agree ✓

### 5. csl-orig untouched — PASS

`git status --porcelain | grep -v vidyut_eval` → empty; 0 changes of any kind outside `vidyut_eval/` (no modifications, no untracked files). `csl-orig` read-only usage confirmed by the pipeline (make_sample.py only reads GRA_TXT).

## Verifier PASS

All five verification points re-derived independently and matched the committed artifacts exactly; zero divergences found.

_Гасунс_
